# Local Gemma 4 via Ollama — no cloud key required
OLLAMA_HOST=http://127.0.0.1:11434
GEMMA_LOCAL_MODEL=gemma4:e2b

# Optional: use gemma4:e4b if the laptop has 16GB RAM
# GEMMA_LOCAL_MODEL=gemma4:e4b

PORT=8080
DEMO_MODE=false
